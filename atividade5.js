/*Questão 05: Escreva uma arrow function chamada maiorNumero que recebe dois
números como parâmetros e retorna o maior deles.*/

const maiorNumero = (x, b) => {
    return Math.max(x, b)
    };
    console.log(maiorNumero(5, 8));